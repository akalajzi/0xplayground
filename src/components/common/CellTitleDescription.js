import React from 'react'

const CellTitleDescription = ({text}) => {
  return <div style={{
    paddingLeft: '16px',
    marginTop: '-12px',
    fontSize: '12px',
    color: 'grey',
  }}>{text}</div>
}

export default CellTitleDescription
